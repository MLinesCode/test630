export interface WeatherResponse {
  // Define aquí las propiedades relevantes de la respuesta de la API
}

export interface CityWeather {
  // Define aquí las propiedades relevantes para mostrar en tu componente
}